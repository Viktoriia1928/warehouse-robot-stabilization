# GitHub Setup Guide

## Suggested repository name
`amr_stage4_cv_nav_map_integration`

## Suggested description
`ROS 2 Jazzy + Gazebo Harmonic warehouse AMR map/world integration without ArUco markers`

## Push to GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin <YOUR_REPO_URL>
git push -u origin main
```
