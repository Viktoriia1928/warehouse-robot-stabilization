# Experimental notebook

This folder keeps the original Colab notebook used during the experimental phase of the project. It is the working scratchpad — it contains exploration, intermediate stabilization variants that did not make it into the final method, and ad-hoc plotting cells.

The production pipeline does not depend on this notebook. For a clean entry point use the scripts in `scripts/` instead.

Kept here for transparency / reproducibility of the experimental work.
